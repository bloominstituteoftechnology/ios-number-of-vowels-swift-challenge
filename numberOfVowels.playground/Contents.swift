import UIKit

func numberOfVowels(str: String, isYAVowel: Bool = false) -> Int {
    
    let nums:int = 0;
   
    
    for char in str {
        
        if char == ("a" || "e" || "i" || "o" || "u")
        nums ++
        }
        
       return nums
}

print
